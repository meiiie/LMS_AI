# Core API
fastapi==0.109.2
uvicorn[standard]==0.27.1
gunicorn==21.2.0
python-multipart==0.0.9
pydantic>=2.9.0
pydantic-settings>=2.1.0

# Database
sqlalchemy==2.0.27
asyncpg==0.30.0
psycopg2-binary==2.9.9
alembic==1.13.1
pgvector==0.2.5

# --- AI CORE STACK (FIXED) ---
langchain>=1.1.0
langchain-core>=1.1.0
langchain-community>=0.4.0
langchain-openai>=1.1.0
langchain-text-splitters>=1.0.0

langgraph>=1.0.0
langgraph-checkpoint>=3.0.0
langgraph-prebuilt>=1.0.0

# LLM Providers
langchain-google-genai>=3.2.0

# Google GenAI SDK (UNIFIED - CHỈ THỊ 26 + Semantic Memory)
# SDK mới thống nhất cho Gemini API - hỗ trợ cả embeddings và vision
# NOTE: Không dùng google-generativeai vì conflict với langchain-google-genai
google-genai>=1.53.0

# Semantic Memory
numpy>=1.24.0
scikit-learn>=1.3.0

# Neural Reranker (SOTA Dec 2025)
sentence-transformers>=3.0.0  # BGE-Reranker for +15-25% retrieval accuracy

# Graph DB
neo4j~=5.28.0

# PDF Processing
pypdf>=4.0.0
pymupdf>=1.26.0  # CHỈ THỊ 26: PDF to image (no external deps, replaces pdf2image+Poppler)
pdf2image>=1.16.3  # Fallback: requires Poppler system package
Pillow>=10.0.0  # CHỈ THỊ 26: Image processing

# Supabase Storage (CHỈ THỊ 26: Hybrid Infrastructure)
supabase>=2.0.0

# Utils
httpx>=0.28.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
slowapi==0.1.9
pyyaml>=6.0.0  # CHỈ THỊ SỐ 16: YAML config for personas

# Testing
pytest>=7.4.4
pytest-asyncio>=0.23.4
pytest-cov>=4.1.0
hypothesis>=6.92.0